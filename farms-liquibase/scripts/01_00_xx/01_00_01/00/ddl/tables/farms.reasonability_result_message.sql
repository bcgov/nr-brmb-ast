CREATE TABLE farms.farm_rsn_result_messages(
    rsn_result_message_id    numeric(10, 0)    NOT NULL,
    reasonability_test_name            varchar(100)      NOT NULL,
    message                            varchar(4000)     NOT NULL,
    message_type_code                  varchar(10)       NOT NULL,
    reasonability_test_result_id       numeric(10, 0)    NOT NULL,
    revision_count                     numeric(5, 0)     DEFAULT 1 NOT NULL,
    who_created                        varchar(30)       NOT NULL,
    when_created                        timestamp(6)      DEFAULT CURRENT_TIMESTAMP NOT NULL,
    who_updated                        varchar(30),
    when_updated                        timestamp(6)      DEFAULT CURRENT_TIMESTAMP
) TABLESPACE pg_default
;



COMMENT ON COLUMN farms.farm_rsn_result_messages.rsn_result_message_id IS 'REASONABILITY RESULT MESSAGE ID is a surrogate unique identifier for REASONABILITY RESULT MESSAGE.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.reasonability_test_name IS 'REASONABILITY TEST NAME is the name of the reasonability test that generated this message.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.message IS 'MESSAGE is the message text that will be displayed to the user for this reasonability test.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.message_type_code IS 'MESSAGE TYPE CODE is a unique code for the object MESSAGE TYPE CODE. Examples of codes and descriptions are ERROR - Error, WARNING - Warning, INFO - Info.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.reasonability_test_result_id IS 'REASONABILITY TEST RESULT ID is a surrogate unique identifier for REASONABILITY TEST RESULT.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.revision_count IS 'REVISION COUNT is a counter identifying the number of times this record as been modified. Used in the web page access to determine if the record as been modified since the data was first retrieved.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.who_created IS 'CREATE USER indicates the user that created the physical record in the database.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.when_created IS 'CREATE DATE indicates when the physical record was created in the database.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.who_updated IS 'UPDATE USER indicates the user that updated the physical record in the database.'
;
COMMENT ON COLUMN farms.farm_rsn_result_messages.when_updated IS 'UPDATE DATE indicates when the physical record was updated in the database.'
;
COMMENT ON TABLE farms.farm_rsn_result_messages IS 'REASONABILITY RESULT MESSAGE contains messages generated by reasonability tests.'
;


CREATE INDEX ix_rrm_mtc ON farms.farm_rsn_result_messages(message_type_code)
 TABLESPACE pg_default
;
CREATE INDEX ix_rrm_rtri ON farms.farm_rsn_result_messages(reasonability_test_result_id)
 TABLESPACE pg_default
;

ALTER TABLE farms.farm_rsn_result_messages ADD 
    CONSTRAINT pk_rrm PRIMARY KEY (rsn_result_message_id) USING INDEX TABLESPACE pg_default 
;
