$kind: http-request
name: Delete Line Item
url: "{{baseUrl}}/lineItems/:lineItemId"
method: DELETE
pathVariables:
  lineItemId: "{{lineItemId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4452672876905602
