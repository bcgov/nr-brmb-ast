$kind: collection
name: Year Configuration Parameters
variables:
  tenantId: "{{vault:{{vaultPrefix}}-tenantId}}"
  clientId: "{{vault:{{vaultPrefix}}-clientId}}"
  clientSecret: "{{vault:{{vaultPrefix}}-clientSecret}}"
