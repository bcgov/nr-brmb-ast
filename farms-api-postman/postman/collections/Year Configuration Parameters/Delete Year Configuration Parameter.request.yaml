$kind: http-request
name: Delete Year Configuration Parameter
url: "{{baseUrl}}/yearConfigurationParameters/:yearConfigurationParameterId"
method: DELETE
pathVariables:
  yearConfigurationParameterId: "{{yearConfigurationParameterId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4500588875995585
