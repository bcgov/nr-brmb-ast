$kind: http-request
name: Delete Crop Unit Conversion
url: "{{baseUrl}}/cropUnitConversions/:cropUnitDefaultId"
method: DELETE
pathVariables:
  cropUnitDefaultId: "{{cropUnitDefaultId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4484505454549081
