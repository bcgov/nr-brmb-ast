$kind: http-request
name: Get Configuration Parameter
url: "{{baseUrl}}/configurationParameters/:configurationParameterId"
method: GET
pathVariables:
  configurationParameterId: "174"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 200\", function () {\r

      \    pm.response.to.have.status(200);\r

      });\r

      \r

      const json = pm.response.json();\r

      \r

      pm.test(\"Response contains @type = ConfigurationParameterModel\",
      function() {\r

      \    pm.expect(json).to.have.property(\"@type\");\r

      \    pm.expect(json['@type']).to.be.equal(\"ConfigurationParameterModel\"\
      );\r

      });"
    language: text/javascript
order: 1745589873838050
