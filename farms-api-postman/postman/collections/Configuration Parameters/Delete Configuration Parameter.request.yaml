$kind: http-request
name: Delete Configuration Parameter
url: "{{baseUrl}}/configurationParameters/:configurationParameterId"
method: DELETE
pathVariables:
  configurationParameterId: "{{configurationParameterId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4500729720462987
