$kind: http-request
name: Delete Benchmark Per Unit
url: "{{baseUrl}}/benchmarkPerUnits/:benchmarkPerUnitId"
method: DELETE
pathVariables:
  benchmarkPerUnitId: "{{benchmarkPerUnitId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4030653170075660
