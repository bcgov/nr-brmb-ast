$kind: http-request
name: Delete Fair Market Value
url: "{{baseUrl}}/fairMarketValues/:fairMarketValueId"
method: DELETE
pathVariables:
  fairMarketValueId: "{{fairMarketValueId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4432847838359664
