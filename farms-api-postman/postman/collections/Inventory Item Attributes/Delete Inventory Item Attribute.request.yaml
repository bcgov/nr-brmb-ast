$kind: http-request
name: Delete Inventory Item Attribute
url: "{{baseUrl}}/inventoryItemAttributes/:inventoryItemAttributeId"
method: DELETE
pathVariables:
  inventoryItemAttributeId: "{{inventoryItemAttributeId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4400608820939119
