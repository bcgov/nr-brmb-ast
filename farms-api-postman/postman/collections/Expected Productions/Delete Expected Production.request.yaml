$kind: http-request
name: Delete Expected Production
url: "{{baseUrl}}/expectedProductions/:expectedProductionId"
method: DELETE
pathVariables:
  expectedProductionId: "{{expectedProductionId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4497263394888532
