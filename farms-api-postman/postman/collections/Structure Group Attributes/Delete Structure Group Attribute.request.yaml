$kind: http-request
name: Delete Structure Group Attribute
url: "{{baseUrl}}/structureGroupAttributes/:structureGroupAttributeId"
method: DELETE
pathVariables:
  structureGroupAttributeId: "{{structureGroupAttributeId}}"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Status code is 204\", function () {\r

      \    pm.response.to.have.status(204);\r

      });"
    language: text/javascript
order: 4465490505103989
